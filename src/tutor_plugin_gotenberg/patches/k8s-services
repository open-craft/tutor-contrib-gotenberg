---
apiVersion: v1
kind: Service
metadata:
  name: gotenberg
spec:
  type: NodePort
  ports:
    - port: {{ GOTENBERG_PORT }}
      targetPort: 3000
      protocol: TCP
  selector:
    app.kubernetes.io/name: gotenberg
